# 这是模板文件，会跟着代码提交，不要往这里填真实的值。
# 复制成 .env.local 再填，那个才是 Vite 真正读取的文件，且已 gitignore。
#
# 两个值都在 Supabase 项目的 Settings → API 页面：
#   VITE_SUPABASE_URL       = Project URL
#   VITE_SUPABASE_ANON_KEY  = Publishable key（新版，sb_publishable_ 开头）
#                             或 anon public key（旧版 JWT，eyJ 开头），两种都支持
#
# 不填也能跑：应用会进入「演示模式」，数据只存本机浏览器，朋友之间不共享。
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
